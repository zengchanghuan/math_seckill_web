# 数学秒杀 - 网页端

智能刷题系统的网页端应用，基于 Next.js + React + TypeScript 构建。

## 🚀 功能特性

- ✅ **刷题训练** - 按主题和难度筛选题目，支持选择题和填空题
- ✅ **智能推荐** - 基于学习画像的个性化题目推荐（薄弱知识点/综合训练/考前冲刺）
- ✅ **学习画像** - 查看学习数据、知识点掌握度、薄弱点分析
- ✅ **数学公式渲染** - 完美支持 LaTeX 数学公式显示
- ✅ **深色模式** - 支持浅色/深色主题切换
- ✅ **响应式设计** - 适配桌面端和移动端

## 📋 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **状态管理**: Zustand
- **HTTP 客户端**: Axios
- **数学公式**: KaTeX + react-katex
- **图标**: Lucide React

## 🛠️ 安装和运行

### 前置要求

- Node.js >= 18.0.0
- npm 或 yarn 或 pnpm
- 后端服务器运行在 `http://localhost:8000`（可配置）

### 安装依赖

```bash
cd math_seckill_web
npm install
# 或
yarn install
# 或
pnpm install
```

### 开发模式

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

访问 [http://localhost:3000](http://localhost:3000)

### 生产构建

```bash
npm run build
npm start
```

## ⚙️ 配置

### 首次使用

1. 打开应用，进入「设置」页面
2. 配置学生ID（如：`student_001`）
3. 配置服务器地址（默认：`http://localhost:8000`）
4. 点击「测试连接」确认后端连接正常
5. 返回「刷题」页面开始使用

### 环境变量（可选）

创建 `.env.local` 文件：

```env
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```

## 📁 项目结构

```
math_seckill_web/
├── app/                    # Next.js App Router 页面
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页
│   ├── drill/             # 刷题页面
│   ├── recommendation/    # 推荐页面
│   ├── profile/           # 画像页面
│   └── settings/          # 设置页面
├── components/            # 可复用组件
│   ├── Layout.tsx         # 布局组件（导航栏）
│   └── MathText.tsx       # 数学公式渲染组件
├── lib/                   # 工具库
│   ├── api/              # API 客户端
│   │   └── client.ts     # Axios 封装
│   └── store/            # 状态管理
│       └── appStore.ts   # 应用配置状态
├── types/                 # TypeScript 类型定义
│   └── index.ts          # 数据模型类型
└── public/               # 静态资源
```

## 🔌 API 集成

网页端通过 RESTful API 与后端通信，主要接口包括：

- `GET /api/questions/stats` - 题库统计
- `GET /api/questions/{questionId}` - 获取题目
- `POST /api/answers/submit` - 提交答案
- `GET /api/student/{studentId}/profile` - 学生画像
- `POST /api/student/recommend` - 个性化推荐

详细 API 文档请参考后端项目文档。

## 🎨 主题定制

项目使用 Tailwind CSS，支持深色模式。主题配置在 `tailwind.config.ts` 中。

### 自定义颜色

修改 `tailwind.config.ts` 中的 `colors.primary` 配置。

## 📱 响应式设计

- **桌面端**: 顶部导航栏，完整功能展示
- **移动端**: 底部导航栏，优化触摸交互

## 🧪 开发

### 代码检查

```bash
npm run lint
```

### 类型检查

```bash
npm run type-check
```

## 🚢 部署

### Vercel（推荐）

1. 将代码推送到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. 配置环境变量（如需要）
4. 自动部署完成

### 其他平台

项目可以部署到任何支持 Next.js 的平台：

- Netlify
- AWS Amplify
- 自建服务器（使用 `npm run build && npm start`）

## 🔗 相关项目

- **移动端**: [math_seckill](../math_seckill) (Flutter)
- **后端**: [math_seckill_server](../math_seckill_server) (FastAPI)

## 📝 许可证

MIT License

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**数学秒杀** - 让数学学习更高效 🚀
